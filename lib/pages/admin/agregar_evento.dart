import 'package:flutter/material.dart';

class AgregarEvento extends StatelessWidget {
  const AgregarEvento({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Agregar Evento'));
  }
}